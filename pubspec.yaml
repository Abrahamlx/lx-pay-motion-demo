name: lx_pay_motion_demo
description: LX Pay splash, dots loader and success checkmark animations.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/lx_pay_logo.png
